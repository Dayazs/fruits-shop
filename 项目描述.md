# 水果商城 — 项目描述

## 一、项目概述

水果商城是一个基于微信小程序的生鲜电商平台，包含**微信小程序端**（C 端用户购物）、**Web 管理后台**（运营管理）以及**Node.js 后端服务**三大模块。项目涵盖商品浏览、购物车、微信支付、订单管理、数据仪表盘等完整电商闭环。

| 模块 | 目标用户 | 技术栈 |
|---|---|---|
| 微信小程序 | 普通消费者 | uni-app (Vue 3) |
| Web 管理后台 | 运营/管理员 | Vue 3 + Element Plus + ECharts |
| 后端 API 服务 | 为两端提供数据 | Node.js + Express + TypeScript + Prisma + MySQL |

---

## 二、技术架构

```
┌─────────────────────────────────────────────────────┐
│  微信小程序 (uniapp/Vue 3)                           │
│  - 首页/分类/购物车/个人中心 (4 Tab)                    │
│  - 商品详情/订单确认/订单列表                           │
│  - 微信支付 (V3 JSAPI)                               │
│  - uni.request + Bearer Token 认证                   │
└──────────────────┬──────────────────────────────────┘
                   │ HTTP/HTTPS
┌──────────────────▼──────────────────────────────────┐
│  Nginx (端口 10041)                                  │
│  - 管理后台静态资源 + SPA fallback                    │
│  - /api/* 和 /uploads/* 反向代理到后端 10040           │
└──────────────────┬──────────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────────┐
│  Express API Server (端口 10040)                     │
│  - JWT 认证中间件 (accessToken 1天)                    │
│  - RBAC 权限控制 (角色-权限模型)                        │
│  - 微信支付 V3 (RSA-SHA256 签名 + AES-256-GCM 解密)     │
│  - Multer + Sharp 图片上传处理                         │
│  - Prisma ORM 操作 MySQL                             │
└──────────────────┬──────────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────────┐
│  MySQL 数据库                                        │
│  - 13 张数据表                                        │
│  - Prisma Schema 管理 + Migration                    │
└─────────────────────────────────────────────────────┘
```

### 核心技术选型与依赖详解

#### 后端 (api/) — 生产依赖

| 依赖 | 版本 | 作用 |
|---|---|---|
| **express** | 4.21 | Node.js 最主流的 Web 框架。提供路由、中间件、请求/响应处理能力，整个 API 服务构建在它之上 |
| **@prisma/client** | 5.22 | Prisma ORM 的客户端库。在代码中以类型安全的方式操作数据库（增删改查），自动生成 TypeScript 类型，杜绝 SQL 注入 |
| **prisma** | 5.22 | Prisma CLI 工具。用于执行 `prisma generate`（生成客户端）、`prisma migrate`（数据库迁移）、`prisma db push`（同步 Schema） |
| **axios** | 1.7 | HTTP 客户端库。用于向微信支付 V3 API 发起请求（JSAPI 统一下单、获取平台证书），支持 Promise/async-await |
| **jsonwebtoken** | 9.0 | JWT（JSON Web Token）库。用于生成和验证 access token（1天过期）和 refresh token（7天过期），实现无状态身份认证 |
| **bcryptjs** | 3.0 | 纯 JS 实现的 bcrypt 哈希库。对管理员密码和用户密码进行哈希存储，不可逆，防止密码泄露后被直接利用 |
| **dotenv** | 16.4 | 环境变量加载库。从 `.env` 文件读取配置（数据库连接串、JWT 密钥、微信支付参数等）注入 `process.env` |
| **multer** | 1.4 | 文件上传中间件。处理 `multipart/form-data` 类型的图片上传请求，将文件写入指定目录 |
| **sharp** | 0.33 | 高性能图片处理库（C++ 底层）。对上传的图片进行压缩/缩放/格式转换，减少存储空间和传输带宽 |
| **cookie-parser** | 1.4 | Cookie 解析中间件。将请求头中的 Cookie 字符串解析为 JS 对象，方便读取 |
| **cors** | 2.8 | CORS 跨域中间件。设置 `Access-Control-Allow-Origin` 等响应头，允许管理后台（不同端口）跨域请求 API |
| **fs-extra** | 11.3 | Node.js 原生 `fs` 模块的增强版。提供更便捷的文件操作 API（如 `ensureDir` 自动创建目录），用于文件上传目录管理 |

#### 后端 — 开发依赖

| 依赖 | 作用 |
|---|---|
| **typescript** | 将 TS 源码编译为 JS，提供类型检查 |
| **ts-node** | 开发时直接运行 TS 文件，无需手动编译，配合 nodemon 实现热重载 |
| **nodemon** | 监听文件变更自动重启 Node 进程，提升开发效率 |
| **@types/express** 等 | Express、cors、multer 等库的 TypeScript 类型声明文件 |

---

#### 管理后台 (admin/) — 生产依赖

| 依赖 | 版本 | 作用 |
|---|---|---|
| **vue** | 3.5 | 前端 MVVM 框架。Composition API + `<script setup>` 语法，响应式数据绑定，组件化开发 |
| **vue-router** | 5.0 | Vue 官方路由库。管理页面跳转、路由守卫（权限校验、未登录重定向）、History 模式 |
| **pinia** | 3.0 | Vue 3 官方状态管理库。集中管理认证状态（token、用户信息、权限列表），替代 Vuex |
| **element-plus** | 2.14 | 企业级 Vue 3 UI 组件库。提供表单、表格、弹窗、下拉菜单、导航等 60+ 组件，快速搭建管理界面 |
| **@element-plus/icons-vue** | 2.3 | Element Plus 配套图标库。提供 300+ SVG 图标（用户、订单、商品、设置等） |
| **axios** | 1.16 | HTTP 客户端库。管理后台调用后端 API 的统一请求工具，配合请求/响应拦截器处理 token 附加和错误处理 |
| **echarts** | 6.0 | 数据可视化图表库。仪表盘页面的折线图、柱状图渲染，支持交互式 tooltip 和自适应缩放 |

#### 管理后台 — 开发依赖

| 依赖 | 作用 |
|---|---|
| **vite** | 新一代前端构建工具。利用浏览器原生 ES Module 实现极速冷启动和 HMR 热更新 |
| **@vitejs/plugin-vue** | Vite 的 Vue 3 插件，支持 `.vue` 单文件组件编译 |
| **vue-tsc** | Vue 的 TypeScript 类型检查工具 |
| **npm-run-all2** | 并行/串行执行多个 npm script（`build` 时同时跑类型检查和构建） |

---

#### 小程序端 (uniapp/) — 内置能力

| 能力/API | 作用 |
|---|---|
| **uni-app 框架** | 基于 Vue 3 的跨端开发框架，一套代码编译到微信小程序、App、H5 等多端 |
| **uni.request** | 封装的 HTTP 请求 API，自动适配各端网络请求，配合 Bearer Token 认证 |
| **uni.requestPayment** | 微信支付调起 API，传入后端返回的支付参数唤起收银台 |
| **uni.switchTab / uni.navigateTo** | 页面跳转 API |
| **uni.setStorageSync / uni.getStorageSync** | 本地持久化存储，用于 token、页面间参数传递 |
| **uni.login** | 微信登录 API，获取临时 code 换取 openid |
| **自定义 TabBar** | 原生 TabBar 不支持角标，用 Vue 组件自绘实现购物车数量角标和选中状态管理 |
| **Vue 3 Composition API** | `ref`、`computed`、`reactive`、`onLoad`、`onShow` 等生命周期 Hook |

---

## 三、数据库设计

### ER 关系图（核心表）

```
users ──1:N── addresses
users ──1:N── carts ──N:1── fruits ──N:1── categories
users ──1:N── orders ──1:N── order_items ──N:1── fruit_skus ──N:1── fruits
users ──1:N── collections ──N:1── fruits
admins ──N:1── roles ──(permissions JSON)
```

### 13 张表

| 表名 | 说明 | 关键字段 |
|---|---|---|
| `users` | C 端用户 | username, password, openid, refresh_token |
| `addresses` | 收货地址 | receiver_name, mobile, province/city/district |
| `categories` | 商品分类 | name, parent_id (支持层级), sort_order |
| `fruits` | 商品主表 | name, description, main_image, deleted_at (软删除) |
| `fruit_skus` | 商品规格 | spec_name, price, stock, sales, image |
| `carts` | 购物车 | user_id, fruit_id, sku_id, quantity |
| `collections` | 收藏 | user_id, fruit_id |
| `orders` | 订单 | order_no (唯一), total_amount, status (0-4) |
| `order_items` | 订单明细 | fruit_name/spec_name (冗余快照), price, quantity |
| `banners` | 轮播图 | image_url, fruit_id (可跳转商品) |
| `admins` | 管理员 | username, password (bcrypt), role_id |
| `roles` | 角色 | name, permissions (JSON 文本数组) |
| `permissions` | 权限定义 | name, value (唯一), parent_value |

### 设计要点

- **订单商品快照**：`order_items` 冗余存储 `fruit_name`、`spec_name`、`price`，即使商品后续修改或下架，订单记录也不受影响
- **软删除**：`fruits.deleted_at` 支持回收站功能，误删可恢复
- **RBAC 权限**：`roles.permissions` 以 JSON 数组存储权限值，支持 `*` 超级管理员通配符
- **库存扣减**：下单时防御性校验库存；取消订单时自动恢复库存

---

## 四、订单状态流转

```
                   ┌──────────┐
                   │ 待付款(0) │
                   └────┬─────┘
              ┌─────────┼─────────┐
              │ 支付成功  │         │ 取消订单
              ▼         │         ▼
        ┌──────────┐    │   ┌──────────┐
        │ 待发货(1) │    │   │ 已取消(4) │
        └────┬─────┘    │   └──────────┘
             │ 管理员发货 │   (自动恢复库存)
             ▼          │
       ┌──────────┐     │
       │ 待收货(2) │     │
       └────┬─────┘     │
            │ 用户确认收货│
            ▼          │
      ┌──────────┐     │
      │ 已完成(3) │◄────┘ (管理员可直接修改状态)
      └──────────┘
```

**关键时间戳自动记录**：
- `pay_time` → 支付时设置
- `ship_time` → 发货时设置
- `finish_time` → 确认收货/完成时设置

---

## 五、微信支付集成

### 支付流程

1. **小程序端**调用 `POST /api/order/pay`，传入 `order_id` 和 `openid`
2. **后端**调用微信支付 V3 API `POST /v3/pay/transactions/jsapi`：
   - 使用商户私钥 (apiclient_key.pem) 进行 RSA-SHA256 签名
   - 构造 Authorization 头（WECHATPAY2-SHA256-RSA2048）
3. 微信返回 `prepay_id`，后端进行**二次签名**生成小程序调起参数
4. 小程序调用 `wx.requestPayment` 拉起支付
5. **双通道确认**：
   - 微信服务器异步回调 `POST /api/order/pay-callback` → 验签 + AES-256-GCM 解密 → 更新订单
   - 小程序端成功回调后主动调 `PATCH /api/order/:id/pay-success`（兜底）

### 回调验签

```
验签消息: timestamp\nnonce\nrawBody\n
验签证书: 微信支付平台证书 (自动从 /v3/certificates 拉取并缓存)
解密方式: AES-256-GCM (用 APIv3 密钥)
```

---

## 六、安全设计

| 层面 | 措施 |
|---|---|
| 认证 | JWT (access token 1天过期)，请求拦截器自动附加 Bearer token |
| 密码 | bcryptjs 哈希存储，不可逆 |
| 接口鉴权 | `authenticate` 中间件校验 token；`isAdmin` 校验管理员身份 |
| 权限控制 | RBAC：角色 → 权限数组，路由守卫 + 接口中间件双重校验 |
| CORS | Express cors 中间件限制允许的来源 |
| 密码强度 | 登录密码最少 6 位 |
| 订单归属 | 所有订单操作校验 `user_id` 归属，防止越权 |
| 金额校验 | 支付回调时比对 `result.amount.total` 与订单 `total_amount`，防止篡改 |
| SQL 注入 | Prisma ORM 参数化查询，杜绝拼接 |

---

## 七、管理后台功能

### 仪表盘（Dashboard）

- 顶部 4 个统计卡片：今日订单数 / 今日销售总额 / 用户总数 / 待处理订单
- 4 个 ECharts 折线图（最近 7 天）：每日订单量、每日销售总额、每日已完成订单、每日新增用户
- 数据通过 SQL `GROUP BY DATE(created_at)` 聚合，缺失日期自动补 0

### 商品管理

- 商品 CRUD + 多规格 (SKU) 管理
- 分类管理（支持层级）
- 软删除 + 回收站（可恢复/永久删除）
- 图片上传（Multer + Sharp 压缩）

### 订单管理

- 订单列表（关键词搜索、状态筛选）
- 订单详情查看
- 发货操作
- 订单编辑（地址、备注、状态）

### 轮播图管理

- Banner CRUD + 排序

### 用户管理

- 用户列表分页查询，支持用户名模糊搜索、状态筛选、注册时间范围过滤
- 编辑用户信息（用户名、邮箱、手机号），用户名唯一性校验
- 强制重置用户密码（bcrypt 哈希后存储，至少 6 位）
- 启用/禁用用户（禁用后该用户无法登录，包含密码登录和微信登录）
- 一键导出 Excel（`exceljs` 流式写入，支持表头样式和数据行边框）

### 权限管理（RBAC）

- **JWT 权限注入**：登录时将 `permissions` 数组写入 JWT payload，中间件无需查库即可校验
- **`requirePermission` 中间件**：接受单个权限值，精确匹配；子权限（如 `goods:add`）独立于父权限（`goods`），两者是且的关系。`*` 通配符为超级管理员拥有全部权限
- **管理员管理**：CRUD 接口 + 前端页面，支持分配/修改角色
- **角色管理**：CRUD 接口 + 前端页面，角色包含一个权限数组（JSON），通过多选复选框配置
- **侧边栏权限过滤**：菜单项根据当前管理员权限集动态显示/隐藏（`v-if="authStore.hasPermission('goods')"` 等）
- 仪表盘对所有已登录管理员开放，不校验额外权限
- **权限表**（`permissions` 表定义，前端通过 `GET /api/admin/permissions` 获取，角色编辑时按父子层级分组展示）：

| 权限值 | 名称 | 父级 | 说明 |
|--------|------|------|------|
| `*` | 超级管理员 | — | 所有操作 |
| `dashboard` | 仪表盘 | — | 控制台/仪表盘 |
| `goods` | 商品管理 | — | 商品管理目录 |
| `goods:add` | 商品添加 | goods | 添加商品 |
| `goods:edit` | 商品编辑 | goods | 编辑商品 |
| `goods:toggle` | 商品上下架 | goods | 上/下架 |
| `category` | 分类管理 | — | 分类管理目录 |
| `category:add` | 分类添加 | category | 添加分类 |
| `category:edit` | 分类编辑 | category | 编辑分类 |
| `category:delete` | 分类删除 | category | 删除分类 |
| `order` | 订单管理 | — | 订单管理目录 |
| `order:edit` | 订单编辑 | order | 编辑订单 |
| `order:delete` | 订单删除 | order | 删除订单 |
| `user` | 用户管理 | — | 用户管理目录 |
| `user:edit` | 用户编辑 | user | 编辑用户 |
| `user:delete` | 用户删除 | user | 删除用户 |
| `site` | 站点管理 | — | 站点配置 |
| `permission` | 角色管理 | — | 角色权限管理 |
| `admin` | 管理员管理 | — | 管理员管理目录 |
| `admin:add` | 管理员添加 | admin | 添加管理员 |
| `admin:edit` | 管理员编辑 | admin | 编辑管理员 |
| `admin:delete` | 管理员删除 | admin | 删除管理员 |
| `banner` | 轮播图管理 | — | 管理轮播图 |
| `banner:add` | 添加轮播图 | banner | 添加轮播图 |
| `banner:edit` | 编辑轮播图 | banner | 编辑轮播图 |
| `banner:delete` | 删除轮播图 | banner | 删除轮播图 |

---

## 八、小程序端功能

| 页面 | 功能 |
|---|---|
| 首页 | 轮播图、分类入口（点击跳转分类页自动选中）、特色商品展示、加入购物车 |
| 分类页 | 左侧分类树 + 右侧商品列表、搜索 |
| 商品详情 | 轮播图、SKU 选择、价格展示、加入购物车、立即购买 |
| 购物车 | 全选/单选、数量加减、删除、合计结算 |
| 确认订单 | 地址选择/新增、订单商品确认、提交订单、微信支付 |
| 订单列表 | 按状态筛选、取消订单、立即支付、确认收货 |
| 个人中心 | 头像/昵称、订单状态入口（5 个状态带角标数量） |

**购物车角标**：全局 Pinia store 管理 `cartCount`，TabBar 组件响应式显示红色角标（最高显示 99+）。

---

## 九、部署架构

```
服务器: 47.105.65.75 (阿里云)

Nginx (10041)
├── /              → /www/wwwroot/fruits-shop-admin/dist/  (管理后台 SPA)
├── /api/*         → proxy_pass http://127.0.0.1:10040     (后端 API)
└── /uploads/*     → proxy_pass http://127.0.0.1:10040     (静态资源)

Node.js PM2 (10040)
└── /www/wwwroot/fruits-shop-api/dist/main.js

MySQL
└── localhost:3306 / fruits-shop
```

**同源策略**：管理后台通过 Nginx 反向代理，`/api` 和 `/uploads` 请求与页面同源（10041），消除跨域问题。小程序端直接请求后端 10040 端口。

---

## 十、技术难点与解决方案

### 1. 微信支付 V3 回调验签失败

**问题**：初始用商户证书 (apiclient_cert.pem) 验证回调签名，始终返回"签名验证失败"，导致支付成功后数据库订单状态无法更新。

**排查过程**：
1. 通过日志确认回调确实到达了服务器，请求头和请求体都正确
2. 检查验签消息格式 `timestamp\nnonce\nrawBody\n`，与微信文档一致
3. 排查证书来源，发现使用了 `PAY_CERT_PATH`（商户证书），而非微信平台证书
4. 查看微信文档确认：回调签名为**微信平台私钥**签发，须用**微信平台公钥**验签

**解决**：
- 实现三级平台证书获取策略：内存缓存（毫秒级命中）→ 本地 PEM 文件（首次兜底）→ `/v3/certificates` API 远程拉取（自动更新）
- 每次回调根据 `wechatpay-serial` 请求头匹配对应序列号的证书
- 增加小程序端 `paySuccess` 主动确认作为双通道兜底（即使回调失败也能更新订单状态）

---

### 2. 小程序客户端未调用 pay-success 兜底接口

**问题**：后端定义了 `PATCH /order/:orderId/pay-success` 端点用于客户端支付成功确认，但小程序在 `wx.requestPayment` 的 `success` 回调中**从未调用该接口**。如果微信服务器回调延迟或失败，订单将永远停留在"待付款"状态。

**解决**：
- 在 `api.js` 中新增 `orderApi.paySuccess(id)` 方法
- 在 `order-confirm.vue` 和 `order-list.vue` 两个支付入口的 `success` 回调中调用
- 使用 `try/catch` 静默忽略错误（不影响用户感知，回调才是权威确认）

---

### 3. 支付回调数据库不同步

**问题**：修复验签后，回调仍无法更新订单状态。

**排查和复现**：
- 查看了编译后 JS (`dist/`) 发现代码是旧版本，根本没重新编译
- TypeScript 源码已修复但 `npx tsc` 未执行，服务器运行的仍是旧的 `dist/` 文件
- 回调返回 HTTP 200 但 `code: 'FAIL'`，微信收到 200 认为回调成功不再重试，订单永远不更新

**解决**：
- 重新执行 `tsc` 编译后端源码
- 修改回调控制器：`SUCCESS` 时返回 HTTP 200，`FAIL` 时返回 HTTP 400，确保微信在失败时重试
- 后续每次改代码后先编译再部署，避免源码和 dist 不一致

---

### 4. 自定义 TabBar 选中状态错乱

**问题**：点击购物车 Tab 后，再点击其他 Tab，页面切换正常但底部高亮仍停留在购物车。

**排查过程**：
- 打印 `getCurrentPages()` 发现 Tab 页面被缓存，切换时不会重新创建
- 打印 `currentIndex` 发现首次创建后就不再更新
- 进一步发现 `handleSwitchTab` 的 `success` 回调修改的是**旧页面**的 TabBar 实例，新页面从缓存恢复时没有重新计算

**解决**：将 `updateCurrentIndex()` 从组件顶层同步执行改为注册到 `onMounted` 和 `onShow` 两个生命周期，确保每次页面显示（无论新建还是从缓存恢复）都通过 `getCurrentPages()` 重新判定当前是哪个 Tab。

---

### 5. 首页分类点击跳转分类页自动选中

**问题**：`wx.switchTab` 不支持 URL queryString 传参（控制台报 `wx.switchTab: url 不支持 queryString`），无法用常规方式传递 `categoryId`。

**解决**：
- 改用 `uni.setStorageSync('targetCategoryId', catId)` 存储目标分类 ID，`switchTab` 不带参数
- 分类页 `onLoad` 和 `onShow` 中通过 `uni.getStorageSync('targetCategoryId')` 读取
- **遇到二次 bug**：首次点击生效，第二次不生效。排查发现 `onShow` 在 `onLoad` 之前执行，且 `onShow` 中过早调用了 `removeStorageSync`，导致 `onLoad` 读不到值
- 修复：将 `removeStorageSync` 移到成功找到并切换分类的 `if` 分支内部，确保只有真正消费了才删除

---

### 6. 管理后台 CORS 跨域 + VITE 环境变量不生效

**问题 1**：管理后台部署到服务器后，从 `47.105.65.75:10041` 请求 `47.105.65.75:10040`，浏览器报 CORS 错误。后端 CORS 仅允许 `localhost:10041`。

**问题 2**：在 `.env` 中配置 `BASE_URL=http://47.105.65.75:10040/api`，但 `import.meta.env.BASE_URL` 日志输出始终为 `/`，API 请求全部走到 `localhost:10041/api/api/admin/login`（双重 `/api`）。

**排查**：
- **CORS**：两个端口不同源，Express 的 `cors` 中间件白名单是 localhost，服务器上是 IP
- **BASE_URL**：Vite 仅暴露 `VITE_` 前缀的变量给客户端。`BASE_URL` 是 Vite 内置变量（值 = `base` 配置，默认 `/`），`.env` 里自定义的值被完全忽略。`||` 运算符把空字符串当作 falsy 走了 fallback `/api`，导致双 `/api`

**解决**：
- CORS：Nginx 反向代理 `/api/*` 和 `/uploads/*` 到后端 10040 端口，管理后台请求完全同源，不再触发跨域
- VITE 变量：`.env` 改为 `VITE_API_BASE_URL=`（空字符串表示相对路径），`request.ts` 中 `||` 改为 `??`（nullish coalescing），空字符串不再回退到 `/api`

---

### 7. Dashboard 图表数据始终为 0

**问题**：后端统计接口返回正常，但前端 ECharts 图表始终显示 0。后端日志显示 SQL 查询有数据，但前端收到的 `weekly` 数组全是 0。

**排查**：
- 后端日志打印 SQL 查询结果，原始数据正确
- 前端打印 API 响应，7 天数据全是 0
- 对比发现后端用 `String(r.date).slice(0, 10)` 做日期匹配，MySQL `DATE()` 函数通过 Node.js 驱动返回的是 JavaScript Date 对象，`String()` 后的前 10 个字符是 `"Mon May 24"` 而非 `"2026-05-24"`
- 日期永远匹配不上，所有填充值回退为 0

**解决**：
- SQL 改用 `DATE_FORMAT(created_at, '%Y-%m-%d')` 直接返回格式化字符串
- 匹配改为 `r.date === dateStr` 精确比较

---

### 8. onLoad 与 onShow 竞态条件

**问题**：分类页 `onLoad`（async）加载分类列表期间，`onShow`（sync）已经执行完毕。`onShow` 读到 storage 中的 `targetCategoryId` 并将其删除，但此时 `categories.value` 还是空数组（数据尚未返回），无法匹配到目标分类。等 `onLoad` 完成数据加载再去读 storage 时，值已被删除。

**解决**：`removeStorageSync` 延迟到成功匹配并切换分类的 `if` 分支内部执行 —— "消费才删除"。这样 `onShow` 在没有分类数据时不会破坏 storage，`onLoad` 加载完成后可以正常消费。

---

### 9. 库存扣减的并发安全性

**问题**：购物车下单时可能多个用户同时购买同一商品，或用户在支付期间库存被其他订单消耗，导致超卖。

**解决**：
- 使用 Prisma `$transaction` 将 "查库存 → 算金额 → 创订单 → 删购物车" 包装在同一个数据库事务中
- 事务内部进行防御性库存检查（即使加入购物车时已扣减，结算时再次确认）
- 取消订单时在事务内恢复库存
- MySQL InnoDB 的行级锁保证事务隔离

---

## 十一、项目亮点总结

### 架构设计

1. **完整电商闭环**：商品浏览 → 购物车/立即购买 → 下单 → 微信支付 → 发货 → 确认收货 → 取消/退款，14 步全流程打通

2. **三端分离 + 统一 API**：小程序、管理后台、后端独立部署，均通过 RESTful API 通信，任何一端可独立升级替换

3. **RBAC 权限体系**：角色-权限模型，父子权限严格且关系，`requirePermission` 单值精确匹配，JWT 注入避免查库，前端按钮显隐 + 后端接口中间件使用相同权限值双重校验，支持 `*` 超级管理员通配符

### 支付系统

4. **双通道支付确认**：微信服务器回调（权威）+ 客户端主动确认（兜底），即使网络波动或回调延迟，也能通过客户端通道即时更新订单状态

5. **三级证书缓存策略**：内存缓存（毫秒级，常规路径）→ 本地文件（首次兜底）→ 远程 API（自动拉取更新），兼顾性能与高可用

6. **微信支付 V3 完整集成**：商户私钥 RSA 签名请求、平台公钥验签回调、AES-256-GCM 解密资源、平台证书自动拉取/缓存/过期检测

### 数据与存储

7. **订单商品快照设计**：`order_items` 表冗余存储 `fruit_name`、`spec_name`、`price`，商品后续修改/下架/调价均不影响历史订单记录

8. **软删除 + 回收站**：`fruits.deleted_at` 字段实现逻辑删除，误删可在回收站中恢复或永久删除，操作可逆

9. **Prisma Type-Safe ORM**：Schema 定义即数据库文档，自动生成类型安全的查询客户端，杜绝 SQL 注入，Schema 变更通过 Migration 追踪

### 管理后台

10. **ECharts 大屏仪表盘**：4 个统计卡片 + 4 个折线图，SQL `GROUP BY DATE()` 聚合 + 服务端日期补齐（缺失日期补 0）+ 响应式自适应

11. **Pinia Auth Store**：集中管理 token / 用户信息 / 权限，登录/退出/401 自动跳转一体化，状态从 localStorage 自动恢复

12. **多规格商品管理**：一个商品支持多个 SKU（不同重量/规格），分别管理价格、库存、销量、图片

13. **用户管理模块**：管理员可查看/筛选/编辑用户信息、强制重置密码（bcrypt 加密）、一键导出 Excel（`exceljs` 流式写入，支持表头样式和数据行边框）

### 小程序端

14. **自定义 TabBar**：Vue 组件自绘 TabBar + 购物车实时角标（Pinia 响应式 `cartCount`），最大显示 `99+`

15. **立即购买**：跳过购物车直接从商品详情页下单，减少用户操作步骤，提升转化率

16. **分类页联动**：首页点击分类 → 分类页自动定位到对应分类并加载商品，通过 localStorage 传参绕过 `switchTab` 的限制

### 部署运维

17. **Nginx 反向代理**：同源部署消除 CORS 跨域问题，SPA History 模式 `try_files` 回退防止刷新 404，`^~` 修饰符确保代理路由优先于正则规则

17. **PM2 进程守护**：Node.js 生产级进程管理，自动重启、日志收集、负载均衡
